<div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header text_<%= fontColor %>">
        <button type="button" class="close js-word-info-close">
            <span >&times;</span>
            <span class="sr-only">Close</span>
        </button>
        <h4 class="modal-titles"><%= label %></h4>
      </div>
      <div class="modal-body">
        <ul class="list-group">
            <li class="list-group-item list-group-item-heading">
                <span class="badge bg_black"><%= volume %></span>
                <strong>total mentions</strong>
            </li>
            <% _.each(sentiment, function(value, type){ %>
            <li class="list-group-item">
                <span class="badge bg_<%= bgColors[type] %>"><%= value %></span>
                <%= type %>
            </li>
            <% }); %>
        </ul>
      </div>
    </div>
</div>
